const FilterSectionWrapper = ({ children }) => {
    return (
        <div className="w-full flex flex-col gap-1.5">
            {children}
        </div>
    )
}

export default FilterSectionWrapper;