import CartContainer from "@/components/cart/CartContainer";

const Cart = () => {
    return (
        <section className="container section_padding">
            <CartContainer/>
        </section>
    )
}

export default Cart;